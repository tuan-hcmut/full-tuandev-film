// CREATE TABLE bookmark ( id INTEGER PRIMARY KEY AUTO_INCREMENT, name VARCHAR(255) NOT NULL, user_id INTEGER NOT NULL, poster_path VARCHAR(255) NOT NULL, vote_average INTEGER NOT NULL, film_id INTEGER NOT NULL, type VARCHAR(255) NOT NULL, createdAt TIMESTAMP DEFAULT NOW(), FOREIGN KEY(user_id) REFERENCES users(id) );
